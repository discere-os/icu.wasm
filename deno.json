{
  "name": "@discere-os/icu.wasm",
  "exports": {
    ".": "./",
    "./side": "./install/wasm/icu-side.wasm",
    "./main": "./install/wasm/icu-main.js"
  },
  "tasks": {
    "build:main": "meson setup build-main --cross-file=scripts/emscripten.cross -Dmain_module=true -Dside_module=false -Dsimd=true --prefix=$PWD/install -Dlibdir=wasm -Dbindir=wasm && meson compile -C build-main icu-main && meson install -C build-main",
    "build:side": "meson setup build-side --cross-file=scripts/emscripten.cross -Dmain_module=false -Dside_module=true -Dsimd=true --prefix=$PWD/install -Dlibdir=wasm -Dbindir=wasm && meson compile -C build-side icu-side && meson install -C build-side && python3 scripts/postinstall.py $PWD/install",
    "build:wasm": "deno task build:main && deno task build:side"
  }
}

